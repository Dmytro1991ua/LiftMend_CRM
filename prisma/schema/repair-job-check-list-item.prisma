model RepairJobChecklistItem {
  id          String   @id @default(uuid())
  repairJobId String
  label       String
  checked     Boolean
  comment     String?
  checkedAt   DateTime
  checkedBy   String
  repairJob RepairJob @relation(fields: [repairJobId], references: [id])
}
